# Test-profile overrides.
#
# ClaudeConfig.validate() fails fast when claude.api.key is missing, which is
# the right behaviour for a running service but stops any @SpringBootTest from
# starting a context. This dummy key lets context tests run without a real
# credential; no test in this suite makes a network call.
claude.api.key=test-key-not-a-real-credential
